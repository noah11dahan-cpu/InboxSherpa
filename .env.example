@'
# App
APP_ENV=dev
API_PORT=8000

# Postgres (docker-compose)
POSTGRES_USER=inboxsherpa
POSTGRES_PASSWORD=inboxsherpa
POSTGRES_DB=inboxsherpa

# SQLAlchemy URL for local docker network (used later)
DATABASE_URL=postgresql+psycopg://inboxsherpa:inboxsherpa@db:5432/inboxsherpa
'@ | Set-Content -Encoding utf8 .env.example
